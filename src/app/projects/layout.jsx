// import '../globals.css'

export const metadata = {
	title: "Projects",
	description: "rival syahmidan projects showcase",
};

export default function ProjectsLayout({ children }) {
	return (
		<html lang='eng'>
			<body>{children}</body>
		</html>
	);
}
